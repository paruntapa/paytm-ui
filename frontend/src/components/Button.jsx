export const Button = ({label})=>{
    return <div>
        <button class="text-lg bg-black hover:bg-gray-800 w-full text-white font-semibold py-2 px-4 rounded">
            {label}
        </button>
    </div>
}